import { Meta } from '@storybook/blocks';

<Meta title="Documentation/Git Commit" />

# Interactive tool to commit to git.

`npm run git:commit`, Enter commit message.

Add's all un committed items to your working branch.
