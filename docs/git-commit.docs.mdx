import { Meta } from '@storybook/blocks';

<Meta title="Documentation/Git Commit" />

# Interactive tool to commit to git.

`yarn git:commit`, Enter commit message.

Add's all un commited items to your working branch.
