import { Meta } from '@storybook/blocks'

<Meta title='Documentation/Variables' />

# Variables

This document outlines the SASS variables used throughout the project. These variables define layout, typography, spacing, transitions, and more to ensure a consistent design system.

## Settings

- browser-context: `16px`
  *Description*: Base value used to calculate rem values for font sizing.

- spacing: `4px`
  *Description*: Global spacing unit for margins, paddings, and other layout needs.

- rem: `true`
  *Description*: Toggle to switch the application’s sizing method to rem (if true) or vw.

- loop: `10`
  *Description*: Manage loops. e.g. padding, spacers etc.

- breakpoints `('sml', 'med', 'lrg', 'xlrg', 'xxlrg')`
  *Description*: Manage breakpoint loops.

- directions `('top', 'right', 'bottom', 'left')`
  *Description*: Manage directions loops e.g. padding-top, border-top- etc.

## Row

- gutter: `16px`
  *Description*: Row Column gutter size left and right.

- columns: `12`
  *Description*: Define the column amount for Column component.

- max-width: `1440px`
  *Description*: Define Row max width before aligning centre, set to false for 100%.

## Icons

Defines the size variants for icons:

- sml: `20px`
- med: `22px`
- lrg: `24px`
- xlrg: `26px`

## Headings

### Font Size

- h6: `15px`
- h5: `20px`
- h4: `25px`
- h3: `30px`
- h2: `35px`
- h1: `40px`
- d2: `50px`
- d1: `60px`

### Line Height

- h6: `15px`
- h5: `20px`
- h4: `25px`
- h3: `30px`
- h2: `35px`
- h1: `40px`
- d2: `50px`
- d1: `60px`

## Body

### Font Size

- sml: `12px`
- med: `14px`
- lrg: `16px`
- xlrg: `18px`

### Line Height

- sml: `12px`
- med: `14px`
- lrg: `16px`
- xlrg: `18px`

## Button

### Font Size

- sml: `12px`
- med: `14px`
- lrg: `16px`
- xlrg: `18px`

### Line Height

- sml: `12px`
- med: `14px`
- lrg: `16px`
- xlrg: `18px`

## Input

- Font Size: `16px`
- Line Height: `16px`

## Label

- Font Size: `16px`
- Line Height: `16px`

## Breakpoints

- sml: `420px`
- med: `768px`
- lrg: `1080px`
- xlrg: `1440px`
- xxlrg: `1920px`

## Viewport Dimensions

- vw.fullscreen: `100vw`
- vh.fullscreen: `100vh`

## Transitions

### Duration

- slow: `600ms`
- med: `400ms`
- fast: `200ms`

### Timing Function

- slow: `cubic-bezier(0.25, 0.3, 0.25, 1)`
- med: `cubic-bezier(0.25, 0.3, 0.25, 1)`
- fast: `cubic-bezier(0, 0, 0.25, 1)`

### Delay

- slow: `600ms`
- med: `400ms`
- fast: `200ms`

## Border Radius

- sml: `4px`
- med: `6px`
- lrg: `8px`
- xlrg: `10px`
- round: `50%`

## Z-Index

- hidden: `-1`
- none: `0`
- sml: `1`
- med: `2`
- lrg: `3`
- xlrg: `4`

This comprehensive set of variables is intended to maintain consistency across layout, typography, and interactions within the application.

See `src/styles/settings/variables.scss` for more info:

Basic Usage

### Variables Function

```scss
@function vars($name, $variant: null) {
  @if $variant {
    @return map.get(map.get($vars, $name), $variant);
  } @else {
    @return map.get($vars, $name);
  }
}
```

### Basic Usage

`padding: vars(settings, spacing);`

### Deep-Get Function

To get deep objects:

```scss
@function deep-get($map, $keys...) {
  $result: $map;

  @each $key in $keys {
    $result: map.get($result, $key);
  }

  @return $result;
}
```

### Basic Usage

`font-size: deep-get(vars(button, font-size), button, "font-size", sml);`
