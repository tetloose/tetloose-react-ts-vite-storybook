import { Meta } from '@storybook/blocks'

<Meta title='TITLE_NAME' />

# Props

See `EXPORT_NAME.types.ts` for more info on props

### Modifiers (optional)

Pass a class down from parent: `string[]`

### Variant (optional)

Button Style variation: `union type`

# Import

```
import { EXPORT_NAME } from '@components'
```

# Component

```
<EXPORT_NAME variants={['variant-name']} modifiers={['class-name']} />
```
