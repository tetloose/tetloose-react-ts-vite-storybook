import { Meta } from '@storybook/blocks'

<Meta title='TITLE_NAME' />

# Usage

```tsx
import { EXPORT_NAME } from '@components'

;<EXPORT_NAME variants={['variant-name']} modifiers={['class-name']} />
```
