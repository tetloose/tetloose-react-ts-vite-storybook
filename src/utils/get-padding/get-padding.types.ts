import type { GlobalSpacers } from '@global/global.types'

export type Padding = {
  padding?: {
    top?: GlobalSpacers
    right?: GlobalSpacers
    bottom?: GlobalSpacers
    left?: GlobalSpacers
  }
}
