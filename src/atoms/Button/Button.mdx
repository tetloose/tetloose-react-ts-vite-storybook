import { Meta } from '@storybook/blocks'

<Meta title='Atoms/Button' />

# Props

See `src/atoms/Button/Button.types.ts` for more info on props

### Modifiers (optional)

Pass a class down from parent: `string[]`

### Variant (optional)

Button Style variation: `union type`

### Type (optional)

Button Type: `union type`

### Children (optional)

Pass child components inside the button: `ReactNode && ReactNode[]`

### Label (optional)

Button text: `string`

# Import

```
import { Button } from '@atoms'
```

# Component

```tsx
<Button variant={'primary'} size={'med'} label={'Button Label'} type={'button'}>
  {children}
</Button>
```
