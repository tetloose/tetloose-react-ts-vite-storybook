import { use{{pascalCase name}} } from '@hooks/use-{{kebabCase name}}/use-{{kebabCase name}}.hooks'
import {{pascalCase name}} from '@layouts/{{kebabCase name}}/{{kebabCase name}}.component'

const {{pascalCase name}}Page = () => {
  const props = use{{pascalCase name}}()

  return <{{pascalCase name}} {...props} />
}

export default {{pascalCase name}}Page
